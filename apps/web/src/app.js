const apiBase = 'http://localhost:4100';
const viewTitles = { dashboard: 'Clinic Dashboard', patients: 'Patient Records', owners: 'Owner Records', visits: 'Visit Timeline', vaccinations: 'Vaccinations', prescriptions: 'Prescriptions', surgery: 'Surgery', hospitalizations: 'Hospitalization & Boarding', diagnostics: 'Diagnostics & Imaging', audit: 'Audit Trail', roadmap: 'Product Roadmap' };
let selectedPatientId = null;
let searchQuery = '';
let latestState = { blueprint: null, summary: null, owners: [], patients: [], visits: [], vaccinationSummary: null, vaccinations: [], prescriptionSummary: null, prescriptions: [], surgerySummary: null, surgeries: [], hospitalizationSummary: null, hospitalizations: [], diagnosticSummary: null, diagnostics: [], auditEvents: [] };

async function fetchJson(path, options = {}) {
  const response = await fetch(`${apiBase}${path}`, { ...options, headers: { 'content-type': 'application/json', ...(options.headers || {}) } });
  const payload = await response.json();
  if (!response.ok) throw new Error(payload.error || `${path} unavailable`);
  return payload;
}
function metric(label, value) { return `<article class="metric"><strong>${value}</strong><span>${label}</span></article>`; }
function setStatus(message, tone = 'ok') { const status = document.querySelector('#form-status'); status.textContent = message; status.style.background = tone === 'error' ? '#be123c' : '#142033'; if (message) window.setTimeout(() => { status.textContent = ''; }, 2800); }
function searchableText(value) { if (value == null) return ''; if (typeof value === 'string' || typeof value === 'number' || typeof value === 'boolean') return String(value); if (Array.isArray(value)) return value.map(searchableText).join(' '); if (typeof value === 'object') return Object.values(value).map(searchableText).join(' '); return ''; }
function matchesSearch(record) { return !searchQuery || searchableText(record).toLowerCase().includes(searchQuery); }
function filtered(items) { return items.filter(matchesSearch); }
function recordCount(items) { return searchQuery ? `<span class="record-count">${items.length} shown</span>` : ''; }
function queueItem(view, title, detail, count, warning = true) { return `<article class="queue-item${warning ? ' warning' : ''}"><header><div><h3>${title}</h3><p>${detail}</p></div><span class="badge">${count}</span></header><button class="text-button" type="button" data-open-view="${view}">Open</button></article>`; }
const modalLabels = { patient: 'New patient', owner: 'New owner', visit: 'New visit', vaccination: 'Record vaccine', prescription: 'Create prescription', surgery: 'Plan surgery', hospitalization: 'Admit patient', diagnostic: 'Record diagnostic' };
function openModalPanel(panel) { if (!panel) return; document.querySelectorAll('.modal-panel.open').forEach((item) => item.classList.remove('open')); panel.classList.add('open'); document.body.classList.add('modal-active'); }
function closeModalPanels() { document.querySelectorAll('.modal-panel.open').forEach((panel) => panel.classList.remove('open')); document.body.classList.remove('modal-active'); }
function openFormModal(type) { openModalPanel(document.querySelector(`[data-modal="${type}-form"]`) || document.querySelector(`form[data-form="${type}"]`)?.closest('.modal-panel')); }
function modalize(panel, title) { panel.classList.add('modal-panel'); if (!panel.querySelector('.modal-titlebar')) panel.insertAdjacentHTML('afterbegin', `<div class="modal-titlebar"><h2>${title}</h2><button class="modal-close" type="button" data-close-modal>Close</button></div>`); }
function prepareModalPanels() { document.querySelectorAll('.sticky-panel').forEach((panel) => { if (!panel.querySelector('form[data-form]')) return; if (panel.classList.contains('patient-side-panel')) { const form = panel.querySelector('form[data-form="patient"]'); const formHeader = form?.previousElementSibling; const formModal = document.createElement('section'); formModal.className = 'panel modal-panel'; formModal.dataset.modal = 'patient-form'; formModal.innerHTML = '<div class="modal-titlebar"><h2>New patient</h2><button class="modal-close" type="button" data-close-modal>Close</button></div>'; if (formHeader) formModal.appendChild(formHeader); if (form) formModal.appendChild(form); document.body.appendChild(formModal); const listHeader = panel.previousElementSibling?.querySelector('.panel-header'); if (listHeader && !listHeader.querySelector('[data-open-form="patient"]')) listHeader.insertAdjacentHTML('beforeend', '<div class="panel-actions"><button class="text-button" type="button" data-open-form="patient">New patient</button></div>'); modalize(panel, 'Patient detail'); return; } modalize(panel, panel.querySelector('.panel-header h2')?.textContent || 'Record'); const firstForm = panel.querySelector('form[data-form]'); const type = firstForm?.dataset.form; const listPanel = panel.previousElementSibling; const listHeader = listPanel?.querySelector('.panel-header'); if (type && listHeader && !listHeader.querySelector(`[data-open-form="${type}"]`)) { listHeader.insertAdjacentHTML('beforeend', `<div class="panel-actions"><button class="text-button" type="button" data-open-form="${type}">${modalLabels[type] || 'New record'}</button></div>`); } }); }
const detailCollections = { owner: 'owners', visit: 'visits', vaccination: 'vaccinations', prescription: 'prescriptions', surgery: 'surgeries', hospitalization: 'hospitalizations', diagnostic: 'diagnostics', audit: 'auditEvents' };
const detailTitles = { owner: 'Owner detail', visit: 'Visit detail', vaccination: 'Vaccination detail', prescription: 'Prescription detail', surgery: 'Surgery detail', hospitalization: 'Hospitalization detail', diagnostic: 'Diagnostic detail', audit: 'Audit event' };
function ensureDetailModal() { let panel = document.querySelector('[data-modal="record-detail"]'); if (!panel) { panel = document.createElement('section'); panel.className = 'panel modal-panel'; panel.dataset.modal = 'record-detail'; panel.innerHTML = '<div class="modal-titlebar"><h2>Record detail</h2><button class="modal-close" type="button" data-close-modal>Close</button></div><div id="record-detail-body" class="record-detail-body"></div>'; document.body.appendChild(panel); } return panel; }
function detailValue(value) { if (value == null || value === '') return '-'; if (Array.isArray(value)) return value.length ? value.map(detailValue).join('<br />') : '-'; if (typeof value === 'object') return Object.entries(value).map(([key, entry]) => `<strong>${key}</strong>: ${detailValue(entry)}`).join('<br />'); return String(value); }
function detailRows(record) { return Object.entries(record).filter(([key]) => !['patient', 'owner', 'visit'].includes(key)).map(([key, value]) => `<div class="detail-row"><span>${key}</span><p>${detailValue(value)}</p></div>`).join(''); }
function openRecordDetail(type, id) { const collection = latestState[detailCollections[type]] || []; const record = collection.find((entry) => entry.id === id); if (!record) return; const panel = ensureDetailModal(); panel.querySelector('.modal-titlebar h2').textContent = detailTitles[type] || 'Record detail'; panel.querySelector('#record-detail-body').innerHTML = detailRows(record); openModalPanel(panel); }
function phaseCard(phase) { return `<article class="card"><div class="badges"><span class="badge">${phase.code}</span><span class="badge">${phase.featureIds.join(', ')}</span></div><h3>${phase.title}</h3><p>${phase.goal}</p><ul>${phase.deliverables.map((item) => `<li>${item}</li>`).join('')}</ul></article>`; }
function domainCard(domain) { return `<article class="card"><div class="badges"><span class="badge">${domain.status}</span><span class="badge">${domain.featureRange}</span></div><h3>${domain.title}</h3><p>${domain.summary}</p><p><strong>${domain.featureCount}</strong> vecori</p></article>`; }
function ownerRow(owner) { return `<article class="record-row"><header><div><h3>${owner.displayName}</h3><p class="record-meta">${owner.phone || 'No phone'} · ${owner.email || 'No email'}</p></div><span class="badge">${owner.language}</span></header><p class="record-meta">${owner.address?.city || 'No city'} · Balance ${(owner.balanceCents / 100).toFixed(2)} EUR</p><div class="record-actions"><div class="badges">${owner.tags.map((tag) => `<span class="badge">${tag}</span>`).join('')}</div><button class="text-button" type="button" data-detail-type="owner" data-detail-id="${owner.id}">Details</button></div></article>`; }
function patientRow(patient) { const allergy = patient.allergies?.[0] ? `<span class="alert">Critical: ${patient.allergies[0].substance}</span>` : '<span class="ok">No critical allergy</span>'; const weight = patient.weightHistory?.at(-1)?.weightKg ? `${patient.weightHistory.at(-1).weightKg} kg` : 'No weight'; const active = patient.id === selectedPatientId ? ' selected' : ''; return `<article class="record-row patient-row${active}" data-patient-card="${patient.id}"><header><div><h3>${patient.name}</h3><p class="record-meta">${patient.species} · ${patient.breed || 'Unknown breed'} · ${weight}</p></div>${allergy}</header><p class="record-meta">Microchip: ${patient.microchip || 'Not registered'} · Passport: ${patient.passportNumber || 'Not issued'}</p><p class="record-meta">Owner: ${patient.owners.map((owner) => owner.displayName).join(', ')}</p><div class="record-actions"><div class="badges">${patient.tags.map((tag) => `<span class="badge">${tag}</span>`).join('')}</div><button class="text-button" type="button" data-patient-id="${patient.id}">Details</button></div></article>`; }
function visitRow(visit) { const total = ((visit.totalCents || 0) / 100).toFixed(2); const signAction = visit.status === 'signed' ? '' : `<button class="text-button" type="button" data-sign-visit-id="${visit.id}">Sign visit</button>`; const amendmentCount = visit.amendments?.length || 0; const amendmentBadge = amendmentCount ? `<span class="badge">${amendmentCount} amendment${amendmentCount === 1 ? '' : 's'}</span>` : ''; const amendments = amendmentCount ? `<div class="amendments">${visit.amendments.map((amendment) => `<p><strong>${amendment.reason}</strong> · ${amendment.note}</p>`).join('')}</div>` : ''; return `<article class="record-row"><header><div><h3>${visit.patient?.name || 'Patient'} · ${visit.visitType}</h3><p class="record-meta">${new Date(visit.startedAt).toLocaleString()} · ${visit.clinician}</p></div><div class="visit-actions"><span class="badge">${visit.status}</span>${amendmentBadge}${signAction}<button class="text-button" type="button" data-detail-type="visit" data-detail-id="${visit.id}">Details</button></div></header><p>${visit.anamnesis || 'No anamnesis yet.'}</p><p class="record-meta">TPR: ${visit.physicalExam.temperatureC || '-'} C · ${visit.physicalExam.pulseBpm || '-'} bpm · ${visit.physicalExam.respirationRpm || '-'} rpm</p><p class="record-meta">Treatment: ${visit.treatmentPlan?.join(', ') || 'No plan yet'} · Total ${total} EUR</p>${amendments}</article>`; }
function vaccinationRow(vaccination) { const statusClass = vaccination.status === 'overdue' ? 'alert' : 'ok'; const action = vaccination.status === 'overdue' ? `<button class="text-button" type="button" data-mark-vaccine-id="${vaccination.id}">Mark current</button>` : ''; return `<article class="record-row"><header><div><h3>${vaccination.patient?.name || 'Patient'} · ${vaccination.vaccineName}</h3><p class="record-meta">${vaccination.protocol || 'No protocol'} · Lot ${vaccination.lotNumber || '-'}</p></div><div class="visit-actions"><span class="${statusClass}">${vaccination.status}</span>${action}<button class="text-button" type="button" data-detail-type="vaccination" data-detail-id="${vaccination.id}">Details</button></div></header><p class="record-meta">Administered: ${vaccination.administeredAt || '-'} · Next due: ${vaccination.nextDueAt || '-'} · Expiry: ${vaccination.expiresAt || '-'}</p><div class="badges"><span class="badge">${vaccination.manufacturer || 'unknown manufacturer'}</span><span class="badge">inventory ${vaccination.inventoryReduced ? 'reduced' : 'pending'}</span><span class="badge">${vaccination.certificateStatus}</span></div></article>`; }
function prescriptionRow(prescription) { const statusClass = prescription.status === 'controlled-review' ? 'alert' : 'ok'; const dose = `${prescription.calculatedDoseMg || 0} mg (${prescription.defaultDoseMgPerKg || 0} mg/kg @ ${prescription.patientWeightKg || 0} kg)`; const signAction = prescription.signedAt ? '' : `<button class="text-button" type="button" data-sign-prescription-id="${prescription.id}">Sign RX</button>`; const alerts = prescription.safetyAlerts?.length ? `<div class="amendments">${prescription.safetyAlerts.map((alert) => `<p><strong>Safety</strong> · ${alert}</p>`).join('')}</div>` : ''; return `<article class="record-row"><header><div><h3>${prescription.patient?.name || 'Patient'} · ${prescription.medicationName}</h3><p class="record-meta">${prescription.catalogCode || 'No catalog code'} · ${prescription.route} · ${prescription.frequency}</p></div><div class="visit-actions"><span class="${statusClass}">${prescription.status}</span>${signAction}<button class="text-button" type="button" data-detail-type="prescription" data-detail-id="${prescription.id}">Details</button></div></header><p class="record-meta">Calculated dose: ${dose} · Duration ${prescription.durationDays || 0} days</p><p class="record-meta">Refill: ${prescription.refillDueAt || 'No refill'} · Compliance: ${prescription.complianceStatus}</p><div class="badges"><span class="badge">${prescription.prescriptionRequired ? 'prescription required' : 'otc'}</span><span class="badge">${prescription.controlledSubstance ? 'controlled' : 'standard'}</span></div>${alerts}</article>`; }
function surgeryRow(surgery) { const statusClass = surgery.riskStatus === 'ready' ? 'ok' : 'alert'; const estimate = ((surgery.estimateCents || 0) / 100).toFixed(2); const checklist = surgery.preOpChecklist?.map((item) => `<span class="badge">${item.done ? 'done' : 'open'}: ${item.label}</span>`).join('') || ''; const readyAction = surgery.riskStatus === 'ready' ? '' : `<button class="text-button" type="button" data-complete-surgery-id="${surgery.id}">Complete checklist</button>`; const recoveryAction = surgery.recoveryStatus === 'monitoring' ? '' : `<button class="text-button" type="button" data-start-recovery-id="${surgery.id}">Start recovery</button>`; return `<article class="record-row"><header><div><h3>${surgery.patient?.name || 'Patient'} · ${surgery.procedureName}</h3><p class="record-meta">${new Date(surgery.scheduledAt).toLocaleString()} · ${surgery.surgeon}</p></div><div class="visit-actions"><span class="${statusClass}">${surgery.riskStatus}</span>${readyAction}${recoveryAction}<button class="text-button" type="button" data-detail-type="surgery" data-detail-id="${surgery.id}">Details</button></div></header><p class="record-meta">Checklist ${surgery.checklistProgress}% · Consent ${surgery.consentStatus} · Recovery ${surgery.recoveryStatus}</p><p class="record-meta">Estimate ${estimate} EUR · Follow-up ${surgery.followUpDueAt || 'not scheduled'}</p><div class="badges">${checklist}</div></article>`; }
function hospitalizationRow(stay) { const statusClass = ['stable', 'boarding-stable', 'discharged'].includes(stay.riskStatus) ? 'ok' : 'alert'; const vitals = stay.latestVitals ? `Temp ${stay.latestVitals.temperatureC || '-'} C · Pulse ${stay.latestVitals.pulseBpm || '-'} · Pain ${stay.latestVitals.painScore ?? '-'}` : 'No vitals yet'; const tasks = stay.treatmentSheet?.map((task) => `<span class="badge">${task.completed ? 'done' : 'open'}: ${task.task}</span>`).join('') || ''; const taskAction = stay.openTaskCount ? `<button class="text-button" type="button" data-complete-stay-tasks-id="${stay.id}">Complete tasks</button>` : ''; const dischargeAction = stay.status === 'discharged' ? '' : `<button class="text-button" type="button" data-discharge-stay-id="${stay.id}">Discharge</button>`; return `<article class="record-row"><header><div><h3>${stay.patient?.name || 'Patient'} · ${stay.cage}</h3><p class="record-meta">${stay.stayType} · ${stay.acuity} · discharge ${stay.dischargePlannedAt ? new Date(stay.dischargePlannedAt).toLocaleString() : 'not planned'}</p></div><div class="visit-actions"><span class="${statusClass}">${stay.riskStatus}</span>${taskAction}${dischargeAction}<button class="text-button" type="button" data-detail-type="hospitalization" data-detail-id="${stay.id}">Details</button></div></header><p class="record-meta">${vitals} · Open tasks ${stay.openTaskCount} · Portal photos ${stay.sharedPhotoCount}</p><p class="record-meta">Owner status: ${stay.ownerVisibleStatus || 'not shared yet'}</p><div class="badges">${tasks}</div></article>`; }
function diagnosticRow(record) { const statusClass = record.riskStatus === 'complete' ? 'ok' : 'alert'; const annotations = record.annotations?.map((annotation) => `<span class="badge">${annotation.label}</span>`).join('') || ''; const thumbAction = record.thumbnailStatus === 'generated' ? '' : `<button class="text-button" type="button" data-generate-thumbnail-id="${record.id}">Generate thumbnail</button>`; const reportAction = record.riskStatus === 'complete' ? '' : `<button class="text-button" type="button" data-finalize-diagnostic-id="${record.id}">Finalize report</button>`; return `<article class="record-row"><header><div><h3>${record.patient?.name || 'Patient'} · ${record.title}</h3><p class="record-meta">${record.modality} · ${new Date(record.capturedAt).toLocaleString()} · ${record.fileName || 'no file name'}</p></div><div class="visit-actions"><span class="${statusClass}">${record.riskStatus}</span>${thumbAction}${reportAction}<button class="text-button" type="button" data-detail-type="diagnostic" data-detail-id="${record.id}">Details</button></div></header><p class="record-meta">Thumbnail ${record.thumbnailStatus} · PACS ${record.pacsLink ? 'linked' : 'not linked'} · AI pending ${record.aiPending}</p><p class="record-meta">Impression: ${record.report?.impression || 'Report open'}</p><div class="badges">${annotations}<span class="badge">${record.annotationCount} annotations</span><span class="badge">${record.mediaCount} media</span></div></article>`; }
function auditRow(event) { return `<article class="record-row"><header><div><h3>${event.summary}</h3><p class="record-meta">${new Date(event.at).toLocaleString()} · ${event.actor}</p></div><div class="visit-actions"><span class="badge">${event.action}</span><span class="badge">${event.entityType}</span></div></header><p class="record-meta">Record: ${event.entityId}</p></article>`; }
function weightHistory(patient) { if (!patient.weightHistory?.length) return '<p class="muted">No weight entries yet.</p>'; return `<div class="mini-table">${patient.weightHistory.map((entry) => `<div><span>${entry.date}</span><strong>${entry.weightKg} kg</strong></div>`).join('')}</div>`; }
function patientTimeline(patient, visits) { const patientVisits = visits.filter((visit) => visit.patientId === patient.id).sort((a, b) => b.startedAt.localeCompare(a.startedAt)); const visitItems = patientVisits.map((visit) => `<article class="timeline-item"><span>${new Date(visit.startedAt).toLocaleDateString()}</span><strong>${visit.visitType}</strong><p>${visit.anamnesis || 'No anamnesis yet.'}</p><small>${visit.status} · ${visit.clinician}${visit.continuityFromVisitId ? ` · continuity from ${visit.continuityFromVisitId}` : ''}</small></article>`).join(''); const vaccineItems = latestState.vaccinations.filter((vaccination) => vaccination.patientId === patient.id).map((vaccination) => `<article class="timeline-item"><span>${vaccination.administeredAt}</span><strong>${vaccination.vaccineName}</strong><p>Next due ${vaccination.nextDueAt} · ${vaccination.status}</p><small>F046-F055 vaccination workflow</small></article>`).join(''); const rxItems = latestState.prescriptions.filter((prescription) => prescription.patientId === patient.id).map((prescription) => `<article class="timeline-item"><span>${prescription.signedAt ? new Date(prescription.signedAt).toLocaleDateString() : 'Draft'}</span><strong>${prescription.medicationName}</strong><p>${prescription.calculatedDoseMg} mg · ${prescription.route} · ${prescription.frequency}</p><small>F056-F067 prescription workflow</small></article>`).join(''); const surgeryItems = latestState.surgeries.filter((surgery) => surgery.patientId === patient.id).map((surgery) => `<article class="timeline-item"><span>${new Date(surgery.scheduledAt).toLocaleDateString()}</span><strong>${surgery.procedureName}</strong><p>${surgery.riskStatus} · checklist ${surgery.checklistProgress}%</p><small>F068-F077 surgery workflow</small></article>`).join(''); const stayItems = latestState.hospitalizations.filter((stay) => stay.patientId === patient.id).map((stay) => `<article class="timeline-item"><span>${new Date(stay.admittedAt).toLocaleDateString()}</span><strong>${stay.cage}</strong><p>${stay.stayType} · ${stay.riskStatus} · ${stay.ownerVisibleStatus}</p><small>F078-F085 hospitalization workflow</small></article>`).join(''); const diagnosticItems = latestState.diagnostics.filter((record) => record.patientId === patient.id).map((record) => `<article class="timeline-item"><span>${new Date(record.capturedAt).toLocaleDateString()}</span><strong>${record.modality}: ${record.title}</strong><p>${record.riskStatus} · ${record.annotationCount} annotations · ${record.thumbnailStatus}</p><small>F086-F094 diagnostics workflow</small></article>`).join(''); const weightItems = patient.weightHistory?.map((entry) => `<article class="timeline-item"><span>${entry.date}</span><strong>Weight recorded</strong><p>${entry.weightKg} kg</p><small>F008 weight trend</small></article>`).join('') || ''; return visitItems || vaccineItems || rxItems || surgeryItems || stayItems || diagnosticItems || weightItems ? `${visitItems}${vaccineItems}${rxItems}${surgeryItems}${stayItems}${diagnosticItems}${weightItems}` : '<p class="muted">No clinical timeline yet.</p>'; }
function renderPatientDetail() { const target = document.querySelector('#patient-detail'); if (!target) return; const patient = latestState.patients.find((entry) => entry.id === selectedPatientId) || latestState.patients[0]; if (!patient) { target.innerHTML = '<div class="empty-detail">Create a patient to see the clinical timeline.</div>'; return; } selectedPatientId = patient.id; const allergyBanner = patient.allergies?.length ? `<div class="danger-banner"><strong>Critical allergy</strong><span>${patient.allergies.map((allergy) => `${allergy.substance}${allergy.note ? `: ${allergy.note}` : ''}`).join(' · ')}</span></div>` : '<div class="safe-banner"><strong>No critical allergies</strong><span>Clinical screens are clear for this patient.</span></div>'; const owners = patient.owners.map((owner) => owner.displayName).join(', ') || 'No linked owner'; target.innerHTML = `<div class="detail-header"><div><p class="eyebrow">Patient detail</p><h2>${patient.name}</h2><p class="record-meta">${patient.species} · ${patient.breed || 'Unknown breed'} · ${patient.status}</p></div><span class="badge">${patient.microchip || 'no-chip'}</span></div>${allergyBanner}<div class="detail-grid"><div><h3>Owners</h3><p class="record-meta">${owners}</p></div><div><h3>BCS</h3><p class="record-meta">${patient.bcs?.score || '-'} / ${patient.bcs?.scale || '1-9'}</p></div></div><h3>Weight trend</h3>${weightHistory(patient)}<h3>Clinical timeline</h3><div class="timeline-detail">${patientTimeline(patient, latestState.visits)}</div>`; }
function formData(form) { return Object.fromEntries(new FormData(form).entries()); }
function fillSelect(select, items, labelKey) { if (!select) return; select.innerHTML = items.map((item) => `<option value="${item.id}">${item[labelKey]}</option>`).join(''); }
function fillVisitSelect(select, visits) { if (!select) return; select.innerHTML = visits.map((visit) => `<option value="${visit.id}">${visit.patient?.name || 'Patient'} · ${visit.visitType} · ${visit.status}</option>`).join(''); }
function switchView(view) { document.querySelectorAll('.nav-item').forEach((item) => item.classList.toggle('active', item.dataset.view === view)); document.querySelectorAll('.view').forEach((panel) => panel.classList.toggle('active', panel.dataset.panel === view)); document.querySelector('#view-title').textContent = viewTitles[view]; }
function bindTabs() { document.querySelectorAll('.nav-item').forEach((button) => button.addEventListener('click', () => switchView(button.dataset.view))); }
function bindTopbar() { document.querySelector('#global-search')?.addEventListener('input', (event) => { searchQuery = event.target.value.trim().toLowerCase(); renderLists(); renderPatientDetail(); renderVaccinations(); renderPrescriptions(); renderSurgeries(); renderHospitalizations(); renderDiagnostics(); renderAudit(); }); document.addEventListener('click', (event) => { const detailButton = event.target.closest('[data-detail-type]'); if (detailButton) { openRecordDetail(detailButton.dataset.detailType, detailButton.dataset.detailId); return; } const viewButton = event.target.closest('[data-open-view]'); if (viewButton) { switchView(viewButton.dataset.openView); return; } const formButton = event.target.closest('[data-open-form]'); if (formButton) { openFormModal(formButton.dataset.openForm); return; } if (event.target.closest('[data-close-modal]')) { closeModalPanels(); return; } if (document.body.classList.contains('modal-active') && !event.target.closest('.modal-panel')) closeModalPanels(); }); document.addEventListener('keydown', (event) => { if (event.key === 'Escape') closeModalPanels(); }); }
function bindVisitActions() { document.querySelector('#visits-list')?.addEventListener('click', async (event) => { const button = event.target.closest('[data-sign-visit-id]'); if (!button) return; const visit = latestState.visits.find((entry) => entry.id === button.dataset.signVisitId); if (!visit) return; await fetchJson(`/clinic/visits/${visit.id}`, { method: 'PATCH', body: JSON.stringify({ status: 'signed', signedBy: visit.clinician, signedAt: new Date().toISOString() }) }); selectedPatientId = visit.patientId; setStatus('Visit signed successfully.'); await render(); }); }
function bindPrescriptionActions() { document.querySelector('#prescription-list')?.addEventListener('click', async (event) => { const button = event.target.closest('[data-sign-prescription-id]'); if (!button) return; const prescription = latestState.prescriptions.find((entry) => entry.id === button.dataset.signPrescriptionId); if (!prescription) return; await fetchJson(`/clinic/prescriptions/${prescription.id}`, { method: 'PATCH', body: JSON.stringify({ signedBy: 'Dr. Demo', signedAt: new Date().toISOString() }) }); selectedPatientId = prescription.patientId; setStatus('Prescription signed successfully.'); await render(); }); }
function bindPatientOpen() { document.querySelector('#patients-list')?.addEventListener('click', (event) => { const button = event.target.closest('[data-patient-id]'); if (!button) return; selectedPatientId = button.dataset.patientId; renderLists(); renderPatientDetail(); openModalPanel(document.querySelector('.patient-side-panel')); }); document.querySelector('#risk-list')?.addEventListener('click', (event) => { const button = event.target.closest('[data-patient-id]'); if (!button) return; selectedPatientId = button.dataset.patientId; switchView('patients'); renderLists(); renderPatientDetail(); openModalPanel(document.querySelector('.patient-side-panel')); }); }
function nextYear(dateText) { const date = new Date(`${dateText}T00:00:00.000Z`); date.setUTCFullYear(date.getUTCFullYear() + 1); return date.toISOString().slice(0, 10); }
function bindWorkflowActions() { document.addEventListener('click', async (event) => { const button = event.target.closest('[data-mark-vaccine-id], [data-complete-surgery-id], [data-start-recovery-id], [data-complete-stay-tasks-id], [data-discharge-stay-id], [data-generate-thumbnail-id], [data-finalize-diagnostic-id]'); if (!button) return; try { if (button.dataset.markVaccineId) { const today = new Date().toISOString().slice(0, 10); const vaccination = latestState.vaccinations.find((entry) => entry.id === button.dataset.markVaccineId); await fetchJson(`/clinic/vaccinations/${button.dataset.markVaccineId}`, { method: 'PATCH', body: JSON.stringify({ status: 'current', administeredAt: today, nextDueAt: nextYear(today), inventoryReduced: true, certificateStatus: 'ready-for-pdf' }) }); selectedPatientId = vaccination?.patientId || selectedPatientId; setStatus('Vaccination marked current.'); } if (button.dataset.completeSurgeryId) { const surgery = latestState.surgeries.find((entry) => entry.id === button.dataset.completeSurgeryId); await fetchJson(`/clinic/surgeries/${button.dataset.completeSurgeryId}`, { method: 'PATCH', body: JSON.stringify({ consentStatus: 'signed', preOpChecklist: (surgery?.preOpChecklist || []).map((item) => ({ ...item, done: true })), status: 'ready' }) }); selectedPatientId = surgery?.patientId || selectedPatientId; setStatus('Surgery checklist completed.'); } if (button.dataset.startRecoveryId) { const surgery = latestState.surgeries.find((entry) => entry.id === button.dataset.startRecoveryId); await fetchJson(`/clinic/surgeries/${button.dataset.startRecoveryId}`, { method: 'PATCH', body: JSON.stringify({ status: 'recovery', recoveryStatus: 'monitoring' }) }); selectedPatientId = surgery?.patientId || selectedPatientId; setStatus('Recovery monitoring started.'); } if (button.dataset.completeStayTasksId) { const stay = latestState.hospitalizations.find((entry) => entry.id === button.dataset.completeStayTasksId); await fetchJson(`/clinic/hospitalizations/${button.dataset.completeStayTasksId}`, { method: 'PATCH', body: JSON.stringify({ treatmentSheet: (stay?.treatmentSheet || []).map((task) => ({ ...task, completed: true })), ownerVisibleStatus: 'Care tasks completed and patient monitored' }) }); selectedPatientId = stay?.patientId || selectedPatientId; setStatus('Care tasks completed.'); } if (button.dataset.dischargeStayId) { const stay = latestState.hospitalizations.find((entry) => entry.id === button.dataset.dischargeStayId); await fetchJson(`/clinic/hospitalizations/${button.dataset.dischargeStayId}`, { method: 'PATCH', body: JSON.stringify({ status: 'discharged', dischargePlannedAt: new Date().toISOString(), ownerVisibleStatus: 'Patient discharged', dischargePlan: stay?.dischargePlan?.length ? stay.dischargePlan : ['Discharged with home-care instructions'] }) }); selectedPatientId = stay?.patientId || selectedPatientId; setStatus('Patient discharged.'); } if (button.dataset.generateThumbnailId) { const record = latestState.diagnostics.find((entry) => entry.id === button.dataset.generateThumbnailId); await fetchJson(`/clinic/diagnostics/${button.dataset.generateThumbnailId}`, { method: 'PATCH', body: JSON.stringify({ thumbnailStatus: 'generated' }) }); selectedPatientId = record?.patientId || selectedPatientId; setStatus('Thumbnail generated.'); } if (button.dataset.finalizeDiagnosticId) { const record = latestState.diagnostics.find((entry) => entry.id === button.dataset.finalizeDiagnosticId); await fetchJson(`/clinic/diagnostics/${button.dataset.finalizeDiagnosticId}`, { method: 'PATCH', body: JSON.stringify({ status: 'reported', thumbnailStatus: 'generated', report: { ...(record?.report || {}), radiologist: record?.report?.radiologist || 'Dr. Demo', impression: record?.report?.impression || 'Reviewed and no urgent abnormality recorded.', finalizedAt: new Date().toISOString() }, aiScreening: (record?.aiScreening || []).map((screening) => ({ ...screening, result: 'not-run' })) }) }); selectedPatientId = record?.patientId || selectedPatientId; setStatus('Diagnostic report finalized.'); } await render(); } catch (error) { setStatus(error.message, 'error'); } }); }
async function submitForm(form) { const type = form.dataset.form; if (type === 'amendment') { const data = formData(form); const visit = latestState.visits.find((entry) => entry.id === data.visitId); if (!visit) throw new Error('Visit is required'); const amendments = [...(visit.amendments || []), { at: new Date().toISOString(), reason: data.reason, note: data.note }]; await fetchJson(`/clinic/visits/${visit.id}`, { method: 'PATCH', body: JSON.stringify({ amendments }) }); selectedPatientId = visit.patientId; form.reset(); closeModalPanels(); setStatus('Amendment u shtua me sukses.'); await render(); switchView('visits'); return; } const data = formData(form); if (type === 'prescription') data.controlledSubstance = data.controlledSubstance === 'true'; const endpoints = { owner: '/clinic/owners', patient: '/clinic/patients', visit: '/clinic/visits', vaccination: '/clinic/vaccinations', prescription: '/clinic/prescriptions', surgery: '/clinic/surgeries', hospitalization: '/clinic/hospitalizations', diagnostic: '/clinic/diagnostics' }; const created = await fetchJson(endpoints[type], { method: 'POST', body: JSON.stringify(data) }); if (type === 'patient') selectedPatientId = created.id; if (type === 'visit' || type === 'vaccination' || type === 'prescription' || type === 'surgery' || type === 'hospitalization' || type === 'diagnostic') selectedPatientId = created.patientId; form.reset(); closeModalPanels(); setStatus(`${type} u ruajt me sukses.`); await render(); if (type === 'patient' || type === 'visit') switchView('patients'); if (type === 'vaccination') switchView('vaccinations'); if (type === 'prescription') switchView('prescriptions'); if (type === 'surgery') switchView('surgery'); if (type === 'hospitalization') switchView('hospitalizations'); if (type === 'diagnostic') switchView('diagnostics'); }
function bindForms() { document.querySelectorAll('form[data-form]').forEach((form) => form.addEventListener('submit', async (event) => { event.preventDefault(); try { await submitForm(form); } catch (error) { setStatus(error.message, 'error'); } })); }
function renderLists() { const patients = filtered(latestState.patients); const owners = filtered(latestState.owners); const visits = filtered(latestState.visits); const riskPatients = filtered(latestState.patients.filter((patient) => patient.allergies?.length)); document.querySelector('#risk-list').innerHTML = riskPatients.map(patientRow).join('') || '<p class="muted">No critical patient signals.</p>'; document.querySelector('#recent-visits').innerHTML = visits.slice(0, 4).map(visitRow).join('') || '<p class="muted">No visits match the current search.</p>'; document.querySelector('#patients-list').innerHTML = `${recordCount(patients)}${patients.map(patientRow).join('') || '<p class="muted">No patients match the current search.</p>'}`; document.querySelector('#owners-list').innerHTML = `${recordCount(owners)}${owners.map(ownerRow).join('') || '<p class="muted">No owners match the current search.</p>'}`; document.querySelector('#visits-list').innerHTML = `${recordCount(visits)}${visits.map(visitRow).join('') || '<p class="muted">No visits match the current search.</p>'}`; }
function renderVaccinations() { const summary = latestState.vaccinationSummary; if (!summary) return; const vaccinations = filtered(latestState.vaccinations); const alerts = filtered(summary.overdueItems); document.querySelector('#vaccination-metrics').innerHTML = [metric('Vaccinations', summary.counts.vaccinations), metric('Overdue', summary.counts.overdue), metric('Inventory reductions', summary.counts.inventoryReduced)].join(''); document.querySelector('#vaccination-coverage').innerHTML = summary.featureCoverage.map((coverage) => `<article class="card"><div class="badges"><span class="badge">${coverage.range}</span></div><h3>${coverage.area}</h3><p>${coverage.description}</p></article>`).join(''); document.querySelector('#vaccination-alerts').innerHTML = alerts.map(vaccinationRow).join('') || '<p class="muted">No overdue vaccine alerts.</p>'; document.querySelector('#vaccination-list').innerHTML = `${recordCount(vaccinations)}${vaccinations.map(vaccinationRow).join('') || '<p class="muted">No vaccinations match the current search.</p>'}`; }
function renderPrescriptions() { const summary = latestState.prescriptionSummary; if (!summary) return; const prescriptions = filtered(latestState.prescriptions); const controlledAlerts = filtered(summary.controlledAlerts); const refillReminders = filtered(summary.refillReminders); document.querySelector('#prescription-metrics').innerHTML = [metric('Prescriptions', summary.counts.prescriptions), metric('Controlled', summary.counts.controlled), metric('Refills', summary.counts.refillReminders)].join(''); document.querySelector('#prescription-coverage').innerHTML = summary.featureCoverage.map((coverage) => `<article class="card"><div class="badges"><span class="badge">${coverage.range}</span></div><h3>${coverage.area}</h3><p>${coverage.description}</p></article>`).join(''); document.querySelector('#prescription-alerts').innerHTML = controlledAlerts.map(prescriptionRow).join('') || '<p class="muted">No controlled drug alerts.</p>'; document.querySelector('#prescription-refills').innerHTML = refillReminders.map(prescriptionRow).join('') || '<p class="muted">No refill reminders.</p>'; document.querySelector('#prescription-list').innerHTML = `${recordCount(prescriptions)}${prescriptions.map(prescriptionRow).join('') || '<p class="muted">No prescriptions match the current search.</p>'}`; }
function renderSurgeries() { const summary = latestState.surgerySummary; if (!summary) return; const surgeries = filtered(latestState.surgeries); const alerts = filtered(summary.alerts); document.querySelector('#surgery-metrics').innerHTML = [metric('Surgeries', summary.counts.surgeries), metric('Consent needed', summary.counts.consentNeeded), metric('Recovery monitoring', summary.counts.recoveryMonitoring)].join(''); document.querySelector('#surgery-coverage').innerHTML = summary.featureCoverage.map((coverage) => `<article class="card"><div class="badges"><span class="badge">${coverage.range}</span></div><h3>${coverage.area}</h3><p>${coverage.description}</p></article>`).join(''); document.querySelector('#surgery-alerts').innerHTML = alerts.map(surgeryRow).join('') || '<p class="muted">No surgery alerts.</p>'; document.querySelector('#surgery-list').innerHTML = `${recordCount(surgeries)}${surgeries.map(surgeryRow).join('') || '<p class="muted">No surgeries match the current search.</p>'}`; }
function renderHospitalizations() { const summary = latestState.hospitalizationSummary; if (!summary) return; const stays = filtered(latestState.hospitalizations); const alerts = filtered(summary.alerts); document.querySelector('#hospitalization-metrics').innerHTML = [metric('Active stays', summary.counts.active), metric('Open tasks', summary.counts.openTasks), metric('Portal photos', summary.counts.sharedPhotos)].join(''); document.querySelector('#hospitalization-coverage').innerHTML = summary.featureCoverage.map((coverage) => `<article class="card"><div class="badges"><span class="badge">${coverage.range}</span></div><h3>${coverage.area}</h3><p>${coverage.description}</p></article>`).join(''); document.querySelector('#hospitalization-alerts').innerHTML = alerts.map(hospitalizationRow).join('') || '<p class="muted">No hospitalization alerts.</p>'; document.querySelector('#hospitalization-list').innerHTML = `${recordCount(stays)}${stays.map(hospitalizationRow).join('') || '<p class="muted">No stays match the current search.</p>'}`; }
function renderDiagnostics() { const summary = latestState.diagnosticSummary; if (!summary) return; const diagnostics = filtered(latestState.diagnostics); const alerts = filtered(summary.alerts); document.querySelector('#diagnostic-metrics').innerHTML = [metric('Diagnostics', summary.counts.diagnostics), metric('Review needed', summary.counts.reviewNeeded), metric('AI pending', summary.counts.aiPending)].join(''); document.querySelector('#diagnostic-coverage').innerHTML = summary.featureCoverage.map((coverage) => `<article class="card"><div class="badges"><span class="badge">${coverage.range}</span></div><h3>${coverage.area}</h3><p>${coverage.description}</p></article>`).join(''); document.querySelector('#diagnostic-alerts').innerHTML = alerts.map(diagnosticRow).join('') || '<p class="muted">No diagnostic alerts.</p>'; document.querySelector('#diagnostic-list').innerHTML = `${recordCount(diagnostics)}${diagnostics.map(diagnosticRow).join('') || '<p class="muted">No diagnostics match the current search.</p>'}`; }
function renderAudit() { const events = filtered(latestState.auditEvents); document.querySelector('#audit-list').innerHTML = `${recordCount(events)}${events.map(auditRow).join('') || '<p class="muted">No audit events match the current search.</p>'}`; }
function renderWorkQueue() { const items = []; const critical = latestState.patients.filter((patient) => patient.allergies?.length).length; if (critical) items.push(queueItem('patients', 'Critical patient signals', 'Review allergy banners before prescribing or procedures.', critical)); if (latestState.vaccinationSummary?.counts.overdue) items.push(queueItem('vaccinations', 'Overdue vaccines', 'Mark current or record updated vaccine events.', latestState.vaccinationSummary.counts.overdue)); if (latestState.prescriptionSummary?.counts.unsignedControlled) items.push(queueItem('prescriptions', 'Controlled RX needs signature', 'Sign regulated prescriptions and keep audit trail current.', latestState.prescriptionSummary.counts.unsignedControlled)); if (latestState.surgerySummary?.alerts.length) items.push(queueItem('surgery', 'Surgery readiness', 'Complete consent, checklist or recovery monitoring.', latestState.surgerySummary.alerts.length)); if (latestState.hospitalizationSummary?.alerts.length) items.push(queueItem('hospitalizations', 'Inpatient care tasks', 'Close nursing tasks or discharge planning gaps.', latestState.hospitalizationSummary.alerts.length)); if (latestState.diagnosticSummary?.alerts.length) items.push(queueItem('diagnostics', 'Diagnostics pending', 'Generate thumbnails or finalize reports.', latestState.diagnosticSummary.alerts.length)); const count = items.length; document.querySelector('#queue-count').textContent = `${count} open`; document.querySelector('#work-queue').innerHTML = items.join('') || '<div class="empty-state">No open clinical work queue items right now.</div>'; }
async function render() { const [blueprint, summary, owners, patients, visits, vaccinationSummary, vaccinations, prescriptionSummary, prescriptions, surgerySummary, surgeries, hospitalizationSummary, hospitalizations, diagnosticSummary, diagnostics, auditEvents] = await Promise.all([fetchJson('/blueprint'), fetchJson('/clinic/summary'), fetchJson('/clinic/owners'), fetchJson('/clinic/patients'), fetchJson('/clinic/visits'), fetchJson('/clinic/vaccinations/summary'), fetchJson('/clinic/vaccinations'), fetchJson('/clinic/prescriptions/summary'), fetchJson('/clinic/prescriptions'), fetchJson('/clinic/surgeries/summary'), fetchJson('/clinic/surgeries'), fetchJson('/clinic/hospitalizations/summary'), fetchJson('/clinic/hospitalizations'), fetchJson('/clinic/diagnostics/summary'), fetchJson('/clinic/diagnostics'), fetchJson('/clinic/audit')]); latestState = { blueprint, summary, owners: owners.items, patients: patients.items, visits: visits.items, vaccinationSummary, vaccinations: vaccinations.items, prescriptionSummary, prescriptions: prescriptions.items, surgerySummary, surgeries: surgeries.items, hospitalizationSummary, hospitalizations: hospitalizations.items, diagnosticSummary, diagnostics: diagnostics.items, auditEvents: auditEvents.items }; if (!selectedPatientId && latestState.patients[0]) selectedPatientId = latestState.patients[0].id; document.querySelector('#api-status').textContent = 'API online'; document.querySelector('#api-status').classList.remove('offline'); fillSelect(document.querySelector('#owner-options'), latestState.owners, 'displayName'); fillSelect(document.querySelector('#patient-options'), latestState.patients, 'name'); fillSelect(document.querySelector('#vaccination-patient-options'), latestState.patients, 'name'); fillSelect(document.querySelector('#prescription-patient-options'), latestState.patients, 'name'); fillSelect(document.querySelector('#surgery-patient-options'), latestState.patients, 'name'); fillSelect(document.querySelector('#hospitalization-patient-options'), latestState.patients, 'name'); fillSelect(document.querySelector('#diagnostic-patient-options'), latestState.patients, 'name'); fillVisitSelect(document.querySelector('#visit-options'), latestState.visits); fillVisitSelect(document.querySelector('#prescription-visit-options'), latestState.visits); fillVisitSelect(document.querySelector('#surgery-visit-options'), latestState.visits); fillVisitSelect(document.querySelector('#hospitalization-visit-options'), latestState.visits); fillVisitSelect(document.querySelector('#diagnostic-visit-options'), latestState.visits); document.querySelector('#metrics').innerHTML = [metric('Patients', summary.counts.patients), metric('Owners', summary.counts.owners), metric('Visits', summary.counts.visits), metric('Signed visits', summary.counts.signedVisits), metric('Critical allergies', summary.counts.criticalAllergies), metric('Overdue vaccines', vaccinationSummary.counts.overdue), metric('RX alerts', prescriptionSummary.counts.unsignedControlled), metric('Surgery alerts', surgerySummary.alerts.length), metric('Care alerts', hospitalizationSummary.alerts.length), metric('Diagnostic alerts', diagnosticSummary.alerts.length), metric('Audit events', auditEvents.items.length)].join(''); document.querySelector('#clinic-summary').innerHTML = summary.featureCoverage.map((coverage) => `<article class="card"><div class="badges"><span class="badge">${coverage.range}</span></div><h3>${coverage.area}</h3><p>${coverage.description}</p></article>`).join(''); renderWorkQueue(); renderLists(); renderPatientDetail(); renderVaccinations(); renderPrescriptions(); renderSurgeries(); renderHospitalizations(); renderDiagnostics(); renderAudit(); document.querySelector('#phases').innerHTML = blueprint.phases.map(phaseCard).join(''); document.querySelector('#domains').innerHTML = blueprint.domains.map(domainCard).join(''); }
prepareModalPanels(); bindTabs(); bindTopbar(); bindForms(); bindPatientOpen(); bindVisitActions(); bindPrescriptionActions(); bindWorkflowActions();
try { await render(); } catch (error) { document.querySelector('#api-status').textContent = 'API offline'; document.querySelector('#api-status').classList.add('offline'); document.querySelector('#metrics').innerHTML = metric('Start API', '4100'); document.querySelector('#clinic-summary').innerHTML = `<article class="card"><h3>Nis API</h3><p>Starto API me <code>npm run dev:api</code>, pastaj rifresko faqen.</p></article>`; setStatus(error.message, 'error'); }
